import cx from 'classnames'

import './{{pascalCase}}.scss'

const {{pascalCase}} = ({
  className,
}) => {
  return (
    <div className={cx('{{pascalCase}}', className)}>
      
    </div>
  );
};

export default {{pascalCase}}
